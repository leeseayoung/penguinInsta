<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.penguin.penguinInsta.mamo.repository.PostRepository">
  	
  	<insert id="insertPost" parameterType="map">
  		INSERT INTO 
  				`post`
  			(
  			
	  			`userId`
				, `title`
				, `content`
						
  			)
  			VALUE
  			(
  				#{userId}
  				, #{title}
  				, #{content}
  				
  			
  			)
  			
  	
  	</insert>
  
  	<select id="selectPostList"
  	resultType="com.penguin.penguinInsta.mamo.domain.Post">
 			 SELECT
 			 	`id`
 			 	, `userId`
				, `content`
				, `imagePath`
				, `createdAt`
				, `updatedAt`
			FROM	 
  				`post`
  			ORDER BY	
  				`id` DESC
  	
  	</select>
  
  
  
	  <select id=" selectPost" parameterType="int"
	  resultType="com.penguin.penguinInsta.mamo.domain.Post">
	  		  SELECT
 			 	`id`
 			 	, `userId`
				, `title`
				, `content`
				, `imagePath`
				, `createdAt`
				, `updatedAt`
			FROM	 
  				`post`
  			WHERE
  					`id` = #{id}
	  
	  
	  
	  
	  
	  
	  </select>
  
  
  
  
  
  
  
  
  	
  
  </mapper>
